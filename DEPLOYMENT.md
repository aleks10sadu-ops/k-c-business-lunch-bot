# Инструкция по развертыванию бота

## 🚫 Почему не Vercel?

Vercel предназначен для **serverless** функций с коротким временем выполнения. Telegram-бот с polling требует **долгоживущий процесс**, который постоянно работает и получает обновления от Telegram.

## ✅ Рекомендуемые платформы

### 1. 🚀 Railway (Рекомендуется)

**Плюсы:** Простое развертывание, бесплатный тариф, автоматический деплой из GitHub

**Шаги:**
1. Зарегистрируйтесь на [Railway.app](https://railway.app)
2. Создайте новый проект
3. Подключите ваш GitHub репозиторий
4. Добавьте переменную окружения:
   - `BOT_TOKEN` = ваш токен бота
5. Railway автоматически обнаружит Dockerfile и запустит бота
6. Готово!

**Стоимость:** Бесплатно до 500 часов/месяц ($5 кредитов)

---

### 2. 🐳 Render.com

**Плюсы:** Бесплатный тариф, автоматический деплой

**Шаги:**
1. Зарегистрируйтесь на [Render.com](https://render.com)
2. Нажмите **"New +"** → **"Web Service"**
3. Подключите ваш GitHub репозиторий
4. Настройки:
   - **Name:** `menu-bot` (или любое другое имя)
   - **Environment:** `Docker` ⚠️ **ВАЖНО: выберите Docker, а не Python 3!**
   - **Region:** выбирайте ближайший к вам
   - **Branch:** `main`
   - **Root Directory:** (оставьте пустым)
   - **Dockerfile Path:** `Dockerfile` (по умолчанию)
   - **Docker Command:** (оставьте пустым - используется из Dockerfile)
   - **Plan:** `Free` (или `Starter` для постоянной работы без "засыпания")
   - **Environment Variables:** 
     - Добавьте переменную: `BOT_TOKEN` = ваш токен бота
5. Нажмите **"Create Web Service"**
6. Render автоматически соберет и запустит ваш бот

**Важно:** 
- ✅ Выбирайте **Docker**, а не Python 3
- На бесплатном тарифе сервис может "засыпать" после 15 минут неактивности (бот перестанет отвечать, но перезапустится при следующем запросе)
- Для постоянной работы нужен платный тариф ($7/месяц для Starter)

**Стоимость:** Бесплатно (с ограничениями) или $7/месяц (Starter)

---

### 3. ☁️ Fly.io

**Плюсы:** Глобальная сеть, хорошая производительность

**Шаги:**
1. Установите [flyctl](https://fly.io/docs/getting-started/installing-flyctl/)
2. Войдите: `fly auth login`
3. Создайте приложение: `fly launch`
4. Установите переменные окружения:
   ```bash
   fly secrets set BOT_TOKEN=ваш_токен
   ```
5. Деплой: `fly deploy`

**Стоимость:** Бесплатно (1 общий CPU, 256MB RAM)

---

### 4. 💻 VPS (DigitalOcean, Hetzner, etc.)

**Плюсы:** Полный контроль, неограниченные ресурсы

**Шаги:**

#### Вариант A: Docker Compose (рекомендуется)

1. Арендуйте VPS (минимум 1GB RAM)
2. Установите Docker и Docker Compose:
   ```bash
   curl -fsSL https://get.docker.com -o get-docker.sh
   sh get-docker.sh
   ```
3. Склонируйте репозиторий:
   ```bash
   git clone https://github.com/aleks10sadu-ops/k-c-business-lunch-bot.git
   cd k-c-business-lunch-bot
   ```
4. Создайте `.env` файл:
   ```bash
   echo "BOT_TOKEN=ваш_токен" > .env
   ```
5. Запустите через Docker Compose:
   ```bash
   docker-compose up -d
   ```
6. Проверьте логи:
   ```bash
   docker-compose logs -f
   ```

#### Вариант B: Напрямую через Python

1. Установите Python 3.9+:
   ```bash
   sudo apt update
   sudo apt install python3 python3-pip python3-venv
   ```
2. Склонируйте репозиторий:
   ```bash
   git clone https://github.com/aleks10sadu-ops/k-c-business-lunch-bot.git
   cd k-c-business-lunch-bot
   ```
3. Создайте виртуальное окружение:
   ```bash
   python3 -m venv venv
   source venv/bin/activate
   ```
4. Установите зависимости:
   ```bash
   pip install -r requirements.txt
   ```
5. Установите переменную окружения:
   ```bash
   export BOT_TOKEN=ваш_токен
   ```
6. Используйте systemd для автозапуска:

   Создайте файл `/etc/systemd/system/menu-bot.service`:
   ```ini
   [Unit]
   Description=Telegram Menu Bot
   After=network.target

   [Service]
   Type=simple
   User=ваш_пользователь
   WorkingDirectory=/path/to/k-c-business-lunch-bot
   Environment="BOT_TOKEN=ваш_токен"
   ExecStart=/path/to/k-c-business-lunch-bot/venv/bin/python -m bot.main
   Restart=always

   [Install]
   WantedBy=multi-user.target
   ```

   Активируйте сервис:
   ```bash
   sudo systemctl daemon-reload
   sudo systemctl enable menu-bot
   sudo systemctl start menu-bot
   sudo systemctl status menu-bot
   ```

**Стоимость:** От $4-6/месяц (Hetzner) или $6/месяц (DigitalOcean)

---

### 5. ☁️ Google Cloud Run / AWS Lambda (webhook режим)

**Примечание:** Требует модификации кода для работы через webhook вместо polling

Для этого нужно:
- Переписать `bot/main.py` для работы с webhook
- Настроить HTTPS endpoint
- Настроить домен

Это более сложный вариант, но возможен для serverless архитектуры.

---

## 🔧 Быстрое развертывание на Railway (рекомендуется)

1. **Перейдите на [Railway.app](https://railway.app)**
2. **Войдите через GitHub**
3. **New Project → Deploy from GitHub repo**
4. **Выберите репозиторий:** `aleks10sadu-ops/k-c-business-lunch-bot`
5. **Добавьте переменную окружения:**
   - Перейдите в Settings → Variables
   - Добавьте: `BOT_TOKEN` = ваш токен от @BotFather
6. **Railway автоматически:**
   - Обнаружит Dockerfile
   - Соберет Docker образ
   - Запустит бота
7. **Проверьте логи:** вкладка Deployments → View Logs

## 📋 Проверка работоспособности

После развертывания:

1. Откройте Telegram
2. Найдите вашего бота
3. Отправьте `/start`
4. Проверьте, что бот отвечает

## 🔍 Мониторинг и логи

- **Railway:** вкладка Deployments → View Logs
- **Render:** вкладка Logs
- **Fly.io:** `fly logs`
- **VPS:** 
  - Docker: `docker-compose logs -f`
  - Systemd: `journalctl -u menu-bot -f`

## 🔄 Обновление бота

1. Сделайте изменения в коде
2. Закоммитьте и запушьте в GitHub:
   ```bash
   git add .
   git commit -m "Update bot"
   git push origin main
   ```
3. Платформа автоматически пересоберет и перезапустит бота

## ⚠️ Важные моменты

1. **Токен бота:** никогда не коммитьте токен в репозиторий, используйте переменные окружения
2. **Файлы:** Убедитесь, что `assets/template.png` и шрифты присутствуют (можно добавить через GitHub или монтировать volume)
3. **Логи:** Проверяйте логи после деплоя для выявления ошибок

## 💰 Сравнение вариантов

| Платформа | Стоимость | Сложность | Рекомендация |
|-----------|-----------|-----------|--------------|
| Railway   | Бесплатно* | ⭐ Просто | ⭐⭐⭐⭐⭐ |
| Render    | Бесплатно* | ⭐ Просто | ⭐⭐⭐⭐ |
| Fly.io    | Бесплатно* | ⭐⭐ Средне | ⭐⭐⭐⭐ |
| VPS       | $4-6/мес  | ⭐⭐⭐ Сложно | ⭐⭐⭐ |
| Vercel    | ❌ Не подходит | - | ❌ |

*Бесплатный тариф с ограничениями

---

**Рекомендация:** Начните с **Railway** — это самый простой способ запустить бота без настройки инфраструктуры.

