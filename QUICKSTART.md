# Быстрый старт

## Минимальная настройка за 5 минут

### 1. Установка зависимостей

```bash
pip install -r requirements.txt
```

### 2. Получение токена бота

1. Откройте Telegram и найдите [@BotFather](https://t.me/BotFather)
2. Отправьте `/newbot` и следуйте инструкциям
3. Скопируйте полученный токен

### 3. Настройка переменной окружения

**Windows (PowerShell):**
```powershell
$env:BOT_TOKEN="ваш_токен_здесь"
```

**Linux/Mac:**
```bash
export BOT_TOKEN="ваш_токен_здесь"
```

### 4. Подготовка ресурсов

**Минимально необходимые файлы:**
- `assets/template.png` - изображение шаблона (размер: **797 × 1132 пикселей**)
- `assets/fonts/Inter.ttf` (или другой шрифт) - файл шрифта

**Если у вас есть PDF-шаблон:**
1. Конвертируйте PDF в PNG с правильным разрешением (см. `PDF_TO_PNG.md`)
2. Размер должен быть точно **797 × 1132 пикселей**
3. Сохраните как `assets/template.png`

**Если у вас нет шаблона:**
1. Создайте изображение размером **797 × 1132 пикселей** (вертикальная ориентация)
2. Сохраните как `assets/template.png`
3. Скачайте любой TTF шрифт и поместите в `assets/fonts/`
4. Обновите пути в `config/settings.yaml` если используете другое имя файла

### 5. Настройка координат (опционально)

Если у вас есть готовый шаблон, обновите `config/zones.yaml` с координатами зон для каждого дня.

Если шаблон еще не готов, можно использовать примерные координаты из файла.

### 6. Запуск

```bash
python -m bot.main
```

### 7. Тестирование

1. Откройте Telegram
2. Найдите вашего бота
3. Отправьте `/start`
4. Отправьте текст меню (пример в `example_menu.txt`)

## Готово! 🎉

Бот должен ответить сгенерированным изображением меню.

## Что дальше?

- Настройте шаблон под свои нужды (см. `SETUP_TEMPLATE.md`)
- Измените шрифты и размеры в `config/settings.yaml`
- Добавьте или измените дни недели

Все изменения производятся через конфигурационные файлы без правки кода!

