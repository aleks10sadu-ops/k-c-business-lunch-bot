# Локальный запуск через Docker

Быстрая инструкция для запуска бота на вашем компьютере через Docker.

## Предварительные требования

1. **Установите Docker Desktop:**
   - Windows/Mac: [Docker Desktop](https://www.docker.com/products/docker-desktop)
   - Linux: `sudo apt install docker.io docker-compose`

2. **Проверьте установку:**
   ```bash
   docker --version
   docker-compose --version
   ```

## Быстрый старт

### 1. Клонируйте репозиторий (если еще не клонировали)

```bash
git clone https://github.com/aleks10sadu-ops/k-c-business-lunch-bot.git
cd k-c-business-lunch-bot
```

### 2. Подготовьте ресурсы

Убедитесь, что у вас есть:
- `assets/template.png` - шаблон изображения (797×1132 px)
- `assets/fonts/Inter.ttf` (или другой шрифт) - файл шрифта

Если файлов нет, создайте директории:
```bash
mkdir -p assets/fonts
mkdir -p output
```

### 3. Создайте файл с токеном

**Windows (PowerShell):**
```powershell
echo "BOT_TOKEN=ваш_токен_здесь" > .env
```

**Windows (CMD):**
```cmd
echo BOT_TOKEN=ваш_токен_здесь > .env
```

**Linux/Mac:**
```bash
echo "BOT_TOKEN=ваш_токен_здесь" > .env
```

**Или создайте файл `.env` вручную:**
```
BOT_TOKEN=ваш_токен_от_BotFather
```

### 4. Запустите бота

```bash
docker-compose up -d
```

Флаг `-d` запускает контейнер в фоновом режиме.

### 5. Проверьте логи

```bash
docker-compose logs -f
```

Вы должны увидеть:
```
2025-12-16 10:38:39,729 - __main__ - INFO - Загрузка конфигурации...
2025-12-16 10:38:39,735 - __main__ - INFO - Конфигурация загружена успешно
2025-12-16 10:38:39,831 - __main__ - INFO - Запуск бота...
```

### 6. Протестируйте бота

1. Откройте Telegram
2. Найдите вашего бота
3. Отправьте `/start`
4. Бот должен ответить

## Управление ботом

### Остановить бота
```bash
docker-compose down
```

### Перезапустить бота
```bash
docker-compose restart
```

### Посмотреть статус
```bash
docker-compose ps
```

### Посмотреть логи (без фона)
```bash
docker-compose logs -f
```

### Остановить и удалить контейнер
```bash
docker-compose down
```

### Пересобрать образ (после изменений в коде)
```bash
docker-compose up -d --build
```

## Решение проблем

### Бот не запускается

1. **Проверьте токен:**
   ```bash
   cat .env
   ```
   Убедитесь, что `BOT_TOKEN` указан правильно.

2. **Проверьте логи:**
   ```bash
   docker-compose logs
   ```

3. **Проверьте наличие файлов:**
   - `assets/template.png` должен существовать
   - `assets/fonts/Inter.ttf` (или другой шрифт) должен существовать

### Ошибка "No such file or directory"

Убедитесь, что вы находитесь в директории проекта:
```bash
pwd  # Linux/Mac
cd   # Windows
```

### Ошибка с портами

Если порт занят (обычно не должно быть, так как бот не открывает порты), вы можете изменить настройки в `docker-compose.yml`.

### Изменить конфигурацию

Файлы `config/settings.yaml` и `config/zones.yaml` монтируются как read-only volume. Чтобы изменить конфигурацию:

1. Отредактируйте файлы локально
2. Перезапустите контейнер:
   ```bash
   docker-compose restart
   ```

### Обновить код

1. Получите последние изменения:
   ```bash
   git pull
   ```

2. Пересоберите и перезапустите:
   ```bash
   docker-compose up -d --build
   ```

## Полезные команды

### Зайти внутрь контейнера
```bash
docker-compose exec bot bash
```

### Посмотреть использование ресурсов
```bash
docker stats
```

### Очистить неиспользуемые образы
```bash
docker system prune -a
```

## Структура проекта в Docker

```
/app/                          # Рабочая директория в контейнере
├── bot/                       # Код бота
├── renderer/                  # Модули рендеринга
├── config/                    # Конфигурация (монтируется из ./config)
├── assets/                    # Ресурсы (монтируется из ./assets)
├── output/                    # Результаты (монтируется из ./output)
└── requirements.txt           # Зависимости
```

## Что монтируется

В `docker-compose.yml` настроены volumes:
- `./config:/app/config:ro` - конфигурация (только чтение)
- `./assets:/app/assets:ro` - ресурсы (только чтение)
- `./output:/app/output` - результаты (чтение/запись)

Это означает, что изменения в `config/` и `assets/` на хосте сразу доступны в контейнере.

## Альтернатива: без docker-compose

Если хотите запустить напрямую через Docker:

```bash
# Соберите образ
docker build -t menu-bot .

# Запустите контейнер
docker run -d \
  --name menu-bot \
  --env-file .env \
  -v "$(pwd)/config:/app/config:ro" \
  -v "$(pwd)/assets:/app/assets:ro" \
  -v "$(pwd)/output:/app/output" \
  --restart unless-stopped \
  menu-bot
```

Но `docker-compose` проще! 😊

