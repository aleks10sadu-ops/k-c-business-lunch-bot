# Инструкция по использованию бота

## 🐳 Запуск бота через Docker

### Шаг 1: Подготовка

1. **Убедитесь, что Docker установлен:**
   ```bash
   docker --version
   docker-compose --version
   ```

2. **Клонируйте репозиторий (если еще не клонировали):**
   ```bash
   git clone https://github.com/aleks10sadu-ops/k-c-business-lunch-bot.git
   cd k-c-business-lunch-bot
   ```

3. **Проверьте наличие файлов:**
   - `assets/template.png` - шаблон изображения (797×1132 px)
   - `assets/fonts/Inter.ttf` - файл шрифта
   - `config/settings.yaml` - настройки
   - `config/zones.yaml` - координаты зон

### Шаг 2: Настройка токена

1. **Откройте файл `.env` в корне проекта**
   - Если файла нет, создайте его: `echo "BOT_TOKEN=" > .env`

2. **Добавьте токен бота:**
   ```
   BOT_TOKEN=ваш_токен_от_BotFather
   ```
   
   Где получить токен:
   - Откройте Telegram
   - Найдите [@BotFather](https://t.me/BotFather)
   - Отправьте `/newbot` и следуйте инструкциям
   - Скопируйте полученный токен

### Шаг 3: Запуск бота

```bash
# Запуск в фоновом режиме
docker-compose up -d

# Или запуск с просмотром логов
docker-compose up
```

### Шаг 4: Проверка работы

1. **Посмотрите логи:**
   ```bash
   docker-compose logs -f
   ```

2. **Должны увидеть:**
   ```
   INFO - Загрузка конфигурации...
   INFO - Конфигурация загружена успешно
   INFO - Настройка обработчиков...
   INFO - Запуск бота...
   ```

3. **Если есть ошибки, проверьте:**
   - Правильность токена в `.env`
   - Наличие файла `assets/template.png`
   - Наличие файла `assets/fonts/Inter.ttf`

---

## 📱 Использование через Telegram

### Первый запуск

1. **Откройте Telegram**
2. **Найдите вашего бота** (по имени, которое вы указали при создании)
3. **Отправьте команду `/start`**

Бот ответит приветственным сообщением с инструкцией.

### Создание изображения меню

#### Формат текста меню

Отправьте боту текст в следующем формате:

```
15.12–19.12

ПН:
1. БОРЩ [говядина, свёкла, сметана]
2. ПЛОВ [рис, курица, морковь]
3. САЛАТ ОВОЩНОЙ [помидоры, огурцы, лук]

ВТ:
1. СУП ЛАПША [куриный бульон, лапша, яйцо]
2. ГРЕЧКА [гречка, курица, лук]
3. САЛАТ ЦЕЗАРЬ [курица, салат, сыр, соус]

СР:
1. БОРЩ ЗЕЛЁНЫЙ [щавель, яйцо, сметана]
2. РИС [рис, говядина, морковь]

ЧТ:
1. СУП ХАРЧО [говядина, рис, томаты]
2. МАКАРОНЫ [макароны, фарш, лук]

ПТ:
БИЗНЕС ЛАНЧЕЙ НЕ БУДЕТ
```

#### Правила формата:

1. **Диапазон дат (опционально):**
   - Укажите в начале или в любом месте текста
   - Формат: `15.12–19.12` или `С 15.12 по 19.12`
   - Будет отображен в оранжевой рамке

2. **Дни недели:**
   - Указывайте в формате: `ПН:`, `ВТ:`, `СР:`, `ЧТ:`, `ПТ:`
   - Поддерживаются только эти дни

3. **Блюда:**
   - Формат: `1. НАЗВАНИЕ [описание]`
   - Номер блюда обязателен
   - Описание в квадратных скобках обязательно

4. **Отсутствие бизнес-ланча:**
   - Если день без меню, укажите:
     ```
     ПТ:
     БИЗНЕС ЛАНЧЕЙ НЕ БУДЕТ
     ```
   - Или с датой:
     ```
     ПТ:
     ДО 12.01.26 БИЗНЕС ЛАНЧЕЙ НЕ БУДЕТ
     ```

### Получение результата

1. **Отправьте текст меню боту**
   - Вставьте или напишите текст меню в чат
   - Отправьте одним сообщением

2. **Дождитесь обработки**
   - Бот покажет индикатор "печатает..."
   - Обычно обработка занимает 2-5 секунд

3. **Получите изображение**
   - Бот отправит готовое изображение меню
   - Изображение будет содержать весь текст, размещенный на шаблоне

### Примеры использования

#### Пример 1: Полное меню на неделю

```
ПН:
1. БОРЩ [говядина, свёкла, сметана]
2. ПЛОВ [рис, курица, морковь]

ВТ:
1. СУП ЛАПША [куриный бульон, лапша]
2. ГРЕЧКА [гречка, курица]

СР:
1. БОРЩ ЗЕЛЁНЫЙ [щавель, яйцо, сметана]

ЧТ:
1. СУП ХАРЧО [говядина, рис, томаты]

ПТ:
1. РАССОЛЬНИК [говядина, огурцы, перловка]
```

#### Пример 2: С диапазоном дат

```
15.12–19.12

ПН:
1. БОРЩ [говядина, свёкла, сметана]
2. ПЛОВ [рис, курица, морковь]

ВТ:
1. СУП ЛАПША [куриный бульон, лапша]

СР:
1. БОРЩ ЗЕЛЁНЫЙ [щавель, яйцо, сметана]

ЧТ:
1. СУП ХАРЧО [говядина, рис, томаты]

ПТ:
БИЗНЕС ЛАНЧЕЙ НЕ БУДЕТ
```

#### Пример 3: Частичное меню (только некоторые дни)

```
ПН:
1. БОРЩ [говядина, свёкла, сметана]

ВТ:
1. СУП ЛАПША [куриный бульон, лапша]

ПТ:
ДО 12.01.26 БИЗНЕС ЛАНЧЕЙ НЕ БУДЕТ
```

### Команды бота

- `/start` - Показать приветственное сообщение и инструкцию
- `/help` - Показать справку по формату меню

---

## 🔧 Управление ботом

### Остановка бота

```bash
docker-compose down
```

### Перезапуск бота

```bash
docker-compose restart
```

### Просмотр логов

```bash
# В реальном времени
docker-compose logs -f

# Последние 100 строк
docker-compose logs --tail=100

# Логи конкретного сервиса
docker-compose logs bot
```

### Обновление бота

1. **Остановите бота:**
   ```bash
   docker-compose down
   ```

2. **Получите обновления:**
   ```bash
   git pull
   ```

3. **Пересоберите и запустите:**
   ```bash
   docker-compose up -d --build
   ```

---

## ❓ Решение проблем

### Бот не отвечает

1. **Проверьте, что бот запущен:**
   ```bash
   docker-compose ps
   ```
   Должен быть статус "Up"

2. **Проверьте логи:**
   ```bash
   docker-compose logs
   ```

3. **Проверьте токен:**
   - Убедитесь, что в `.env` правильный токен
   - Проверьте, что токен не содержит лишних пробелов

### Ошибка "Шаблон не найден"

1. **Проверьте наличие файла:**
   ```bash
   ls assets/template.png
   ```

2. **Проверьте размер файла:**
   - Должен быть 797 × 1132 пикселей

### Ошибка "Шрифт не найден"

1. **Проверьте наличие файла:**
   ```bash
   ls assets/fonts/Inter.ttf
   ```

2. **Проверьте путь в конфиге:**
   - Откройте `config/settings.yaml`
   - Проверьте путь к шрифту

### Текст не помещается или размещается не там

1. **Проверьте координаты:**
   - Откройте `config/zones.yaml`
   - Убедитесь, что координаты соответствуют вашему шаблону

2. **Проверьте размер шрифта:**
   - Откройте `config/settings.yaml`
   - Попробуйте уменьшить размеры шрифтов

3. **Проверьте количество блюд:**
   - Максимум блюд указан в `config/settings.yaml`
   - По умолчанию: 7 блюд в день

### Изображение не генерируется

1. **Проверьте формат текста:**
   - Убедитесь, что соблюден формат меню
   - Каждое блюдо должно иметь номер, название и описание в []

2. **Проверьте логи:**
   ```bash
   docker-compose logs | grep -i error
   ```

---

## 📝 Сохранение результатов

Сгенерированные изображения сохраняются в директории `output/` на хосте (благодаря volume в docker-compose.yml).

Чтобы посмотреть сохраненные изображения:
```bash
ls output/
```

---

## 🎨 Настройка внешнего вида

Все настройки находятся в `config/settings.yaml`:

- **Размеры шрифтов** - измените `fonts.title.size` и `fonts.description.size`
- **Отступы** - измените `layout.line_spacing` и `layout.dish_spacing`
- **Цвета блоков** - настройте `date_block` и `warning_block`
- **Координаты зон** - измените `config/zones.yaml`

После изменения настроек перезапустите бота:
```bash
docker-compose restart
```

---

## ✅ Чек-лист перед использованием

- [ ] Docker установлен и работает
- [ ] Файл `.env` создан и содержит `BOT_TOKEN=ваш_токен`
- [ ] Файл `assets/template.png` существует (797×1132 px)
- [ ] Файл `assets/fonts/Inter.ttf` существует
- [ ] Бот запущен через `docker-compose up -d`
- [ ] Бот отвечает на `/start` в Telegram

Готово! Теперь вы можете использовать бота для генерации изображений меню! 🎉

