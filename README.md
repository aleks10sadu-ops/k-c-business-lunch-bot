# Telegram-бот для генерации изображений меню бизнес-ланчей

Telegram-бот, который принимает текст меню и автоматически генерирует красивое изображение на основе графического шаблона.

## 📋 Описание проекта

Бот предназначен для автоматизации создания изображений меню бизнес-ланчей. Пользователь отправляет текстовое меню в определенном формате, а бот возвращает готовое изображение с аккуратно размещенным текстом.

## ✨ Возможности

- 📝 Парсинг текста меню по дням недели
- 🎨 Автоматическое размещение текста в заданных зонах изображения
- 🔤 Настраиваемые шрифты, размеры и стили
- 📏 Автоматический перенос строк и контроль высоты
- ⚙️ Полная настройка через конфигурационные файлы без правки кода

## 🏗 Архитектура проекта

```
project/
│
├── bot/
│   ├── main.py              # точка входа Telegram-бота
│   ├── handlers.py          # обработчики сообщений
│   ├── parser.py            # парсер текста меню
│
├── renderer/
│   ├── image_renderer.py    # генерация изображения
│   ├── text_layout.py       # перенос строк, расчёт высоты
│
├── config/
│   ├── settings.yaml        # ВСЕ НАСТРОЙКИ
│   ├── zones.yaml           # координаты зон дней
│   └── loader.py            # загрузчик конфигурации
│
├── assets/
│   ├── template.png         # фон-шаблон
│   ├── fonts/
│       └── Inter.ttf        # шрифты (добавьте свой)
│
├── output/
│   └── result.png           # сгенерированные изображения
│
├── requirements.txt
└── README.md
```

## 🚀 Установка и запуск

### Локальная установка

1. **Клонируйте репозиторий или скопируйте файлы проекта**

2. **Создайте виртуальное окружение:**
```bash
python -m venv venv
```

3. **Активируйте виртуальное окружение:**
- Windows:
```bash
venv\Scripts\activate
```
- Linux/Mac:
```bash
source venv/bin/activate
```

4. **Установите зависимости:**
```bash
pip install -r requirements.txt
```

5. **Настройте переменные окружения:**
```bash
# Windows (PowerShell)
$env:BOT_TOKEN="your_telegram_bot_token"

# Linux/Mac
export BOT_TOKEN="your_telegram_bot_token"
```

6. **Подготовьте ресурсы:**
   - Поместите файл шаблона изображения в `assets/template.png`
   - Добавьте шрифты в `assets/fonts/` (например, `Inter.ttf`)

7. **Запустите бота:**
```bash
python -m bot.main
```

## 📄 Формат шаблона

Бот использует шаблон размером **768 × 1638 пикселей** (вертикальная ориентация).

Если у вас есть PDF-файл шаблона:
- Конвертируйте его в PNG с нужным разрешением
- Размер должен быть: **768 × 1638 пикселей**
- Сохраните как `assets/template.png`
- Подробная инструкция: см. `PDF_TO_PNG.md`

Координаты зон для текста уже настроены в `config/zones.yaml` согласно разметке шаблона.

## 📝 Формат входного текста

Отправьте боту текст меню в следующем формате:

```
ПН:
1. БОРЩ [говядина, свёкла, сметана]
2. ПЛОВ [рис, курица, морковь]

ВТ:
1. СУП ЛАПША [куриный бульон, лапша]
2. ГРЕЧКА [гречка, курица]
```

**Правила формата:**
- День недели указывается в формате: `ПН:`, `ВТ:`, `СР:`, `ЧТ:`, `ПТ:`
- Каждое блюдо начинается с номера: `1.`, `2.`, и т.д.
- Название блюда идет после номера
- Описание блюда указывается в квадратных скобках `[описание]`
- Каждое блюдо должно иметь название и описание

## ⚙️ Настройка

### Изменение шаблона изображения

1. Подготовьте шаблон:
   - Если это PDF: конвертируйте в PNG с размером 768×1638 px - см. `PDF_TO_PNG.md`
   - Если это PNG: убедитесь, что размер 768×1638 пикселей
   - Замените файл `assets/template.png`

2. Обновите координаты зон в `config/zones.yaml` (если изменился размер или разметка):

```yaml
ПН:
  x: 30              # X координата начала зоны (от левого края)
  y: 140             # Y координата начала зоны (от верхнего края)
  width: 110         # Ширина зоны
  max_height: 190    # Максимальная высота текста
```

**Текущая разметка:**
- ПН/ВТ/СР - верхняя часть (y: 140)
- ЧТ/ПТ - нижняя часть (y: 360)

### Изменение шрифтов и размеров

Отредактируйте `config/settings.yaml`:

```yaml
fonts:
  title:
    file: assets/fonts/Inter.ttf
    size: 10.05              # Размер шрифта для названия
    uppercase: true          # Преобразование в верхний регистр
  
  description:
    file: assets/fonts/Inter.ttf
    size: 8.1                # Размер шрифта для описания
```

### Изменение количества блюд

В `config/settings.yaml`:

```yaml
menu:
  max_dishes_per_day: 7      # Максимальное количество блюд в день
```

### Добавление нового дня

1. Добавьте день в `config/settings.yaml`:
```yaml
menu:
  days: ["ПН", "ВТ", "СР", "ЧТ", "ПТ", "СБ"]
```

2. Добавьте зону в `config/zones.yaml`:
```yaml
СБ:
  x: 1660
  y: 220
  width: 300
  max_height: 260
```

3. Обновите шаблон изображения для размещения новой зоны

### Изменение межстрочных интервалов

В `config/settings.yaml`:

```yaml
layout:
  line_spacing: 4      # Межстрочный интервал (пиксели)
  dish_spacing: 6      # Отступ между блюдами (пиксели)
```

## 🚀 Развертывание

Для развертывания бота на сервере см. подробную инструкцию в [`DEPLOYMENT.md`](DEPLOYMENT.md).

**Рекомендуемые платформы:**
- **Railway.app** (рекомендуется) — простой деплой через GitHub
- **Render.com** — бесплатный хостинг
- **Fly.io** — глобальная сеть
- **VPS** (DigitalOcean, Hetzner) — полный контроль

**⚠️ Vercel не подходит** — бот использует долгоживущий процесс (polling), а не serverless функции.

## 🐳 Локальный запуск через Docker

Для запуска на вашем компьютере см. [`LOCAL_DOCKER.md`](LOCAL_DOCKER.md).

**Быстрый старт:**
1. Создайте `.env` файл: `echo "BOT_TOKEN=ваш_токен" > .env`
2. Запустите: `docker-compose up -d`
3. Проверьте логи: `docker-compose logs -f`

## 🐳 Docker (опционально)

См. `Dockerfile` в корне проекта.

Запуск через Docker Compose:

```bash
# Создайте .env файл с BOT_TOKEN
echo "BOT_TOKEN=your_token" > .env

# Запустите
docker-compose up -d

# Проверьте логи
docker-compose logs -f
```

## 📖 Команды бота

- `/start` - Инструкция по использованию
- `/help` - Справка по формату меню

## 🔧 Структура конфигурации

### settings.yaml

Содержит все основные настройки:
- Параметры бота (токен)
- Настройки меню (дни, лимиты)
- Конфигурация шрифтов
- Параметры layout (отступы, интервалы)
- Пути к файлам (шаблон, вывод)

### zones.yaml

Содержит координаты зон для каждого дня недели:
- `x`, `y` - координаты начала зоны
- `width` - ширина зоны
- `max_height` - максимальная высота текста

## 🛠 Требования

- Python 3.9+
- Telegram Bot Token (получить у [@BotFather](https://t.me/BotFather))

## 📦 Зависимости

- `aiogram` - библиотека для работы с Telegram Bot API
- `Pillow` - работа с изображениями
- `PyYAML` - чтение конфигурационных файлов

## 🐛 Решение проблем

**Ошибка "Шрифт не найден":**
- Убедитесь, что файл шрифта находится в `assets/fonts/`
- Проверьте путь в `config/settings.yaml`

**Ошибка "Шаблон не найден":**
- Убедитесь, что файл шаблона находится в `assets/template.png`
- Проверьте путь в `config/settings.yaml`

**Текст размещается не в тех местах:**
- Если вы используете PDF-шаблон, убедитесь, что конвертировали его в PNG с правильным размером
- Размер PNG должен быть точно **768 × 1638 пикселей**
- См. инструкцию по конвертации в `PDF_TO_PNG.md`

**Текст не помещается в зону:**
- Уменьшите `max_dishes_per_day` в настройках
- Уменьшите размеры шрифтов
- Уменьшите `line_spacing` и `dish_spacing`
- Увеличьте `max_height` в `zones.yaml`

**Бот не отвечает:**
- Проверьте, что переменная окружения `BOT_TOKEN` установлена
- Убедитесь, что токен корректен
- Проверьте логи в файле `bot.log`

## 📄 Лицензия

См. файл LICENSE

## 👤 Автор

Проект создан для автоматизации генерации изображений меню бизнес-ланчей.

## 🤝 Вклад

Проект разработан с учетом расширяемости. Все изменения конфигурации можно производить без правки кода.

